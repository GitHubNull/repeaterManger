1. 让插件支持Form Data格式的报文中的会话替换功能以支持Form Data格式报文的越权测试功能
2. 请求管理下的左上方的表格数据支持备注和颜色标记，并且支持滚动条（上下左右都支持），支持显示字段筛选，但是必要的字段ID,method,domain, path 不能隐藏
3. 越权测试模式下支持，用户可以选择请求历史记录的某一条记录然后鼠标右键菜单进行点击进行原始报文和用户会话报文的比对功能，可以进行字符串级别的比对，也能进行字节级别比对（hex格式比对），比对时支持同步滚动，并且默认显示报文相似度（包括请求和响应报文的相似度），比对页也分请求报文和响应报文的比对，默认请求报文和响应报文比对分别在不同的标签页下，也能使用4分格的方式进行比对，这种情况下下左边是请求报文，右边是响应报文，各自又都是上下结构，上面是原始报文下面是用户会话报文，并且左右和上下都能拖动调整显示面积占比